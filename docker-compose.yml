version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: traceurl-web
    ports:
      - "3000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
